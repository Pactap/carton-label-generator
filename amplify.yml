version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step required for static site"
    build:
      commands:
        - echo "Static site - no build needed"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
